<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        {{#if user}}
            <a class="navbar-brand fs-4" >Olá {{#pNome user}}{{/pNome}}!</a>
        {{else}}
            <a class="navbar-brand fs-4" >Olá Visitante!</a>
        {{/if}}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item px-2">
                    <a class="nav-link active" aria-current="page" href="/">Início</a>
                </li>
                <li class="nav-item px-2">
                    <a class="nav-link active" aria-current="page" href="/avaliacoes">Avaliações</a>
                </li>
                {{#unless user}}
                    <li class="nav-item px-2">
                        <a class="nav-link active" aria-current="page" href="/login">Entrar</a>
                    </li>
                {{else}}
                    <li class="nav-item px-2">
                        <a class="nav-link active" aria-current="page" href="/logout">Sair</a>
                    </li>
                {{/unless}}
            </ul>
        </div>
    </div>
</nav>